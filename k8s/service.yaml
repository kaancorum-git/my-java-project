apiVersion: v1
kind: Service
metadata:
  name: portfolio-service
  labels:
    app: portfolio-app
spec:
  type: ClusterIP
  selector:
    app: portfolio-app
  ports:
    - name: http
      port: 8081
      targetPort: http